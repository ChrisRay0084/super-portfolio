"use client"

import { motion } from "framer-motion"

export default function GlassPanels() {
  return (
    <div className="absolute inset-0 pointer-events-none">

      {/*
      <motion.div
        className="absolute top-[25%] left-[10%] w-[200px] h-[120px]
        bg-white/5 border border-cyan-400/20 backdrop-blur-md rounded-lg"
        animate={{ y: [-10, 10, -10] }}
        transition={{ duration: 8, repeat: Infinity }}
      />
      

      <motion.div
        className="absolute bottom-[20%] right-[12%] w-[220px] h-[140px]
        bg-white/5 border border-green-400/20 backdrop-blur-md rounded-lg"
        animate={{ y: [10, -10, 10] }}
        transition={{ duration: 10, repeat: Infinity }}
      />

      */}
    </div>
  )
}